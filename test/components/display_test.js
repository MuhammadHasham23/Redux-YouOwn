import {renderComponent,expect} from '../test_helper';
import Display from '../../src/components/display.js';

describe('videos',()=>{
  let component;
  beforeEach(()=>{
    component = renderComponent(Display);
  });
  // More tests to be written soon . . . 
});
